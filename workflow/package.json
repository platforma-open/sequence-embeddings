{
  "name": "@platforma-open/milaboratories.sequence-embeddings.workflow",
  "version": "1.2.0",
  "private": true,
  "description": "Block Workflow",
  "type": "module",
  "scripts": {
    "build": "shx rm -rf dist && pl-tengo build",
    "format": "/usr/bin/env emacs --script ./format.el || echo 'No emacs.'",
    "check": "pl-tengo check"
  },
  "dependencies": {
    "@platforma-open/milaboratories.protein-embeddings-assets": "catalog:",
    "@platforma-open/milaboratories.sequence-embeddings.software": "workspace:*",
    "@platforma-sdk/workflow-tengo": "catalog:"
  },
  "devDependencies": {
    "@platforma-sdk/tengo-builder": "catalog:",
    "@platforma-sdk/test": "catalog:",
    "shx": "catalog:"
  },
  "peerDependencies": {}
}
